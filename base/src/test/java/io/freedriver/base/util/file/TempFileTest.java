package io.freedriver.base.util.file;

public class TempFileTest {
/*
    @Test
    public void testImageLoad() throws IOException {
        PathImageStream is = new PathImageStream(Paths.get("/home/luna/family_01.png"));
        Path wasAt = null;
        try (TempFile tempFile = is.tempFile()) {
            assertTrue(Files.exists(tempFile.getPath()));
            wasAt = tempFile.getPath();
        } catch (Exception e) {
            e.printStackTrace();
            fail("Can't load");
        }
        assertFalse(Files.exists(wasAt), wasAt.toString() + " still exists");
    }


    @Test
    public void testImageNotify() throws IOException, InterruptedException {
        NotifySend ns = new NotifySend("Help Cats", "Help cats like these, pet them");
        ns.setAppName("Autonomy");
        ns.setIcon("/home/luna/family_01.png");
        ns.display();

        Thread.sleep(1000);
    }
*/
}
